<Window x:Class="ActivityDiagram.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:View="clr-namespace:ActivityDiagram.View"
        xmlns:ViewModel="clr-namespace:ActivityDiagram.ViewModel"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        mc:Ignorable="d ignore"
        Height="420"
        Width="400"
        Title="MVVM Light Application"
        DataContext="{Binding Main, Source={StaticResource Locator}}">


    <Grid x:Name="LayoutRoot" Width="400" >

        <StackPanel Orientation="Horizontal" Background="#FFF18D8D" Height="20" VerticalAlignment="Top" HorizontalAlignment="Right" Width="{Binding Width, ElementName=LayoutRoot}" Grid.ColumnSpan="2">
            <View:Menu/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.ColumnSpan="2">
            <View:LeftPanel/>
            <Grid Width="133" Margin="0,20,0,0" Background="#5FDAD8">
                <ItemsControl ItemsSource="{Binding Rectangles}"/>
            </Grid>
            <View:RightPanel/>

        </StackPanel>
    </Grid>
</Window>
